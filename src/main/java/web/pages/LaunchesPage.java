package web.pages;

import org.openqa.selenium.WebDriver;

/**
 *   Страница запусков (Launches).
 */
public class LaunchesPage extends BasePage{

    public LaunchesPage(WebDriver driver) {
        super(driver);
    }
}
